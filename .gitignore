# Xcode
*.xcuserstate
*.xcworkspace
xcuserdata/
build/
# 仅忽略 xcodeproj 内除 pbxproj / 共享 Scheme 外的内容（否则克隆后无 Scheme，Cmd+R 无法 Run）
*.xcodeproj/*
!*.xcodeproj/project.pbxproj
!*.xcodeproj/xcshareddata/
!*.xcodeproj/xcshareddata/xcschemes/
!*.xcodeproj/xcshareddata/xcschemes/*.xcscheme

# macOS
.DS_Store
*.log
.AppleDouble
.LSOverride

# Swift Package Manager
Packages/
.swiftpm/

# CocoaPods
Pods/
Docs/superpowers
# App
*.app
*.dSYM

# Cache directories
.derivedData/
.WallHavenCache/
.WallHavenImageCache/

# Editor / AI 助手说明（仅本地，不提交、不推送；pre-commit 亦禁止 -f 强加）
AGENTS.md
CLAUDE.md
# 其他常见 AI 编辑器说明（若放在仓库根目录）
GEMINI.md
.cursorrules
COPILOT.md

# Cursor 本地规则与索引（勿入库）
.cursor/

# Editor
.1code/
.claude/
.vscode/
.mimocode/
.workbuddy/
*.swp
*~

# Config (local only)
LocalConfig.json
*.local.json

Docs/*.md
WebWallpaperDemo/
# Temp

# Linux 壁纸引擎 CLI（macOS 项目无用，死文件）
linux-wallpaperengine-cli
*.tmp
.gstack/

# Landing page build output (CI generates these)
Docs/assets/
Docs/index.html
landing/dist/
landing/node_modules/

# Trae
.trae/
# Zcode
.zcode/

# SteamCMD：随仓库提供 `Resources/steamcmd/`（见该目录 README），以便克隆/CI 可打包；勿将用户 Application Support 下的自更新缓存提交进来。
/steamcmd/
Resources/steamcmd/logs/
Resources/steamcmd/package/
Resources/steamcmd/public/
# 注意：Breakpad.framework 和 *.dylib 是 steamcmd 运行时依赖，需要保留在仓库

# Wallpaper Engine：材质包不提交（构建时嵌入 CLI；本地保留或 CI 用 WAIFUX_WE_ASSETS_PACK_URL 拉取）
Resources/assets/

# Wallpaper Engine CLI (web wallpaper daemon)
# wallpaperengine-cli.swift
# Resources/wallpaperengine-cli

# Embedded SteamKit2 service build output
/build/SteamService/
SteamService/bin/
SteamService/obj/

# Node (root level) — playwright 临时测试，不提交
node_modules/
package-lock.json
package.json

# .NET shared framework：目录名 Microsoft.NETCore.App 以 .app 结尾，
# 会被上方 `*.app` 规则误伤 → 整个 shared/ 不进 git，CI 产物缺 .NET 框架
# （表现为生产包 Steam 服务「正在启动」后静默失败）。
# 必须放在 *.app 之后；negation 重新包含目录与其全部内容。
!SteamService/prebuilt/*/runtime/shared/
!SteamService/prebuilt/*/runtime/shared/**
# 仅排除构建产物目录，框架本身的 .o/.a 等中间文件属于运行时必需，一并保留
